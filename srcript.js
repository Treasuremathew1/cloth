ScrollReveal().reveal(".home-text", {
    ...scrollRevealOption,
    origin: "left",
  });

  const scrollRevealOption = {
    distance: "50px",
    origin: "bottom",
    duration: 1000,
  };